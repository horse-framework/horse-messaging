namespace Test.Mq
{
    public class ResponseTest
    {
        //todo: response to server from client (with timeout)
        //todo: response to client from server (with timeout)
        //todo: response to client from another client (with timeout)
        //todo: response to channel from client (with timeout)
    }
}